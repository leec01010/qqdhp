<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Android 11+ 需要声明才能检测微信是否安装 -->
    <queries>
        <package android:name="com.tencent.mm" />
    </queries>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.FamilyDialer">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ContactDetailActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ManageActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".AddEditContactActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".flow.FlowEditorActivity"
            android:screenOrientation="portrait" />

        <!-- 微信视频无障碍服务 -->
        <service
            android:name=".WeChatVideoService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- 浮窗坐标录制服务 -->
        <service
            android:name=".flow.FlowRecordOverlayService"
            android:exported="false" />

    </application>

</manifest>
